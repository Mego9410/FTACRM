# Supabase — from the project's API settings
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000
CRON_SECRET=change-me

# Microsoft Graph (optional — email/calendar sync stays dormant until set)
MS_TENANT_ID=
MS_CLIENT_ID=
MS_CLIENT_SECRET=
ENCRYPTION_KEY=

# Email provider (not linked in this build — campaigns queue but do not send)
# RESEND_API_KEY=
# RESEND_WEBHOOK_SECRET=
# CAMPAIGN_FROM_DOMAIN=

# AI call capture (optional — features stay dormant until set)
# THREECX_WEBHOOK_SECRET=   # shared secret the 3CX CRM template sends in x-webhook-secret
# DEEPGRAM_API_KEY=         # transcription (EU endpoint)
ANTHROPIC_API_KEY=
AI_MODEL_REASONING=claude-sonnet-5
